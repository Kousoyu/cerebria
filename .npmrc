# Trusted publishing configuration
provenance=true
access=public
registry=https://registry.npmjs.org/

# Ensure proper publishing
ignore-scripts=false
fund=false

# For GitHub Actions publishing
//registry.npmjs.org/:_authToken=${NPM_TOKEN}